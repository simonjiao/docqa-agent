# FOLLOWUP

## 1. 系统负责人补充说明

系统定位为“证据驱动的文档问答”。PDF 先解析成页面、元素、block、chunk、table 等可追溯产物；LLM 只基于检索证据组织答案，证据不足必须拒答。

LLM 增强会优先放在三个位置：

- 问答组织：坚持 LLM 辅助、无抽取式回退。
- 页面校订：增强 OCR，整理段落、列表和明显错别字，但不覆盖原始识别结果。
- 表格问答：表格先形成 `table_markdown/table_json`；上一轮已补表格意图加权和 JSON 转 Markdown，避免正文片段抢占和 rows 被截断。

流程上保持“解析产物先落盘，再问答和复核”。核心交付物是 `manifest/pages/elements/edges/blocks/chunks/tables/reviews` 这组可审计数据，便于定位 OCR 错误、表格误判和证据不足。

外部规则会作为长期重点。国标、检测报告、表单、合同等应有独立规则包，覆盖页眉页脚、页码、标准号、图表、公式、字段别名和忽略区域；规则要版本化、可测试，并记录命中的 rule id。

安全合规上，API Key、模型地址和客户路径只放 `.env` 或密钥系统；LLM 只接收必要证据片段；生产按文档密级选择外部模型、私有模型或网关审计；答案必须展示来源和自检，低置信或规则冲突进入人工复核。

## 2. 工作方式补充确认

AI 辅助开发，但不把 AI 输出当验证结果。流程是：先读代码和产物，复现问题，再用 AI 辅助梳理假设、生成局部实现或测试草案；最终以本地命令、接口返回、截图、测试和提交记录为准。

上一轮的具体做法是：

- 用当前国标 PDF 复现 OCR、表格识别和 QA 问题。
- 对“表1检索不到表格”“外表面误触发表格意图”补检索测试，避免只修当前样例。
- 保留 QA JSON、截图和评估输出，演示材料能看到答案、来源和自检。
- 将 LLM 慢响应、OCR/表格风险写入 `docs/debug_trace.md`。
- 文档改动做 Markdown/diff 校验；代码改动跑 pytest 和评估脚本。

AI 可以加速探索、重构和文案整理，但需求边界、事实来源、测试结果、安全合规和最终交付质量必须由负责人承担。
