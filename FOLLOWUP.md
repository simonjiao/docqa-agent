# FOLLOWUP

本次补充实际用时：约 30 分钟。范围是核对两份补充题、现有系统文档和验证输出，补齐负责人视角与工作方式回答；本次没有重做作业或改业务代码。

## 1. 系统负责人补充说明

### 1.1 如果这套系统后续由我负责

当前系统为“证据驱动的文档问答原型”，未达到生产交付的程度。
可用的部分包括：PDF 类型判断、扫描件 OCR、元素图谱产物、block/chunk 检索、基于 mini-agent 的 LLM 证据答复、无答案拒答、自检结果和人工复核记录。上一轮已经能用样例国标 PDF 跑通正文问题、表格问题和无答案问题，并保留 `manifest/pages/elements/edges/blocks/chunks/tables/reviews` 等可追溯产物。

交付前需要完善主要有四类：第一，本地OCR能力的建设；第二，复杂表格结构化是重点风险；第三，章节层级、条款归属、表格归属和跨页内容关系目前靠规则和版面启发式，覆盖面不够；第四，评估集还小，无法证明对客户真实文档稳定。

下一步优先做三件事：接入LLM辅助处理更多初处理材料，扩展评估，把正文、表格、无答案、低质量扫描、跨页归属拆成明确指标；再补强章节层级和表格结构化，因为它们直接影响检索证据；最后补交付 runbook，包括 OCR/模型环境检查、缓存清理、失败降级、人工复核和回滚策略。

### 1.2 如果要给客户试用

先和产品、客户或技术负责人确认：试用文档类型、页数规模、扫描/文本层比例、是否含敏感数据、典型问题清单、允许的模型部署方式、响应时间要求、答案必须引用到什么粒度，以及哪些问题必须拒答而不是猜。

第一版交付范围需要可验证闭环：限定 1-2 类文档，支持上传、解析、问答、来源展示、自检和人工复核；明确不承诺复杂表格全自动正确、跨文档推理、批量生产吞吐和无人工兜底。验收标准用样例问题说清楚。

### 1.3 如果要保证质量和稳定性

三层验证：代码层内置测试用例，产物层检查完整性和反查关系，效果层跑固定问题集和人工标注的 bad case。对客户试用，还要加环境预检，例如 Tesseract 语言包、模型地址、API Key、端口、缓存目录和样本文档解析状态。

如果演示或生产前出问题，先止损而不是盲改：复现最小 case，判断是否影响主流程；如果是低置信 OCR 或表格结构不可信，就把该答案降级为“需要人工复核/证据不足”；如果是服务或模型不可用，先切到已验证的缓存样例或备用模型配置。

### 1.4 如果时间有限

交付风险排序：先保证服务能启动、样本文档能解析、核心问题能回答、来源和拒答逻辑正确；其次处理最影响效果的章节/表格检索问题；最后再做 UI 打磨和泛化能力。风险同步要具体到影响、概率、兜底方案和截止时间，例如复杂元素试用期先进入人工复核队列。

资源协调上，任务并行拆分：补数据和标注，修解析或检索，演示和验收脚本。关键路径严密追踪，尤其是问题复现、修复取舍、测试结论和对外口径；这些不能只靠口头确认。

### 1.5 我会如何使用 AI 或智能体工具

AI/Agent做加速型工作：梳理代码路径、生成测试草案、归纳日志、整理bad case、起草文档、尝试局部重构方案、对比 prompt 或检索策略。AI输出都需要通过事实或测试验证。

人的工作内容包括：需求边界、事实来源、客户数据合规、密钥处理、评估指标、最终答案是否被证据支持、测试是否真的跑过。检查方式是看命令输出、接口返回、截图、JSON 产物、diff 和提交记录；涉及效果优化时，还要在固定评估集上比较前后结果，避免只修一个样例。

### 1.6 补充一个真实例子

表格问答不稳定。最初“表 1 中合格质量水平 AQL 和哪些检查项目有关？”容易被普通正文片段干扰，表格 JSON 直接塞给 LLM 时也可能因为前缀和嵌套字段占空间，导致真正的 rows 内容不突出。

判断：这不是单纯 prompt 问题，而是“表格结构化产物、检索意图和 LLM 输入格式”共同影响。处理时我先保留可追溯的 `table_json/table_markdown`，再对表格意图问题提高 table chunk 权重，并把结构化表格转换成更紧凑的 Markdown 证据；同时补检索和 QA 相关测试，防止“外表面”这类普通问题误触发表格意图。最后用 pytest、评估脚本和演示截图证明正文、表格、无答案问题都能跑通。

### 1.7 如果文档章节层级解析不对

定位错误发生在哪一层：OCR 文本是否错、阅读顺序是否错、block 合并是否错、标题/条款规则是否错，还是 chunk 构建时丢了父子关系。排查时会对比页面图、OCR 行 bbox、`elements.jsonl`、`edges.jsonl`、`blocks.jsonl` 和最终 chunk，确认是单页问题、跨页问题，还是某类文档通用问题。

修复方案会分短期和长期。短期交付时，对低置信章节或跨页归属加 warning，不让它作为确定来源；前端展示页码、片段和人工复核入口，避免错误层级直接污染答案。长期会建立章节/条款 golden set，按标题编号、字号位置、缩进、页眉页脚、表格前后关系、跨页延续等规则做版本化解析；每次调整都要检查 chunk 的父标题、表格归属和来源边是否仍可反查。

### 1.8 如果系统效果不稳定

当前最可能影响效果的问题是“表格和复杂版面的证据质量”。评价标准不能只看 LLM 回答像不像，而要拆成：表格区域是否检测到、行列/单元格是否恢复、table chunk 是否被检索到、答案是否覆盖正确字段、无证据时是否拒答、来源是否能反查。

收集 bad case，并按原因分类：OCR 误识别、表格结构错误、章节归属错误、检索没有命中、证据被截断、LLM 没按证据拒答。优化尝试包括 cell 级 OCR、表格专用 chunk、BM25/embedding 混合检索、rerank、紧凑证据格式和更明确的拒答规则。判断是否有效只看固定评估集和新增 bad case 的前后对比，同时确认正文问题、无答案问题没有回退。

## 2. 工作方式补充确认

### 2.1 遇到不清楚但又比较急的需求

先确认四件事：目标用户和真实场景、最小可验收结果、明确不做的边界、失败时的兜底方式。用 3-5 个样例问题或截图把理解写成短确认，让客户快速反馈。

第一版会做成最短闭环，不追求一次覆盖所有情况。例如先保证“上传一个样例 PDF -> 解析 -> 问答 -> 显示来源 -> 能拒答”跑通。避免做偏，频繁拿真实样例校验；每个阶段都留下可检查产物，比如接口返回、测试结果、截图或评估 JSON。

### 2.2 关键节点前发现问题

先判断修复的影响，避免扩大损失，制定最小修复方案，可能时临时方案，紧盯修复流程，不影响演示为准。确实是阻断项，先做止损方案，例如禁用风险功能、固定演示样例、切备用配置、把低置信答案改成人工复核, 再安排修复。

同步风险时讲清楚“事实、假设、决策时间和候选方案”。要事事有回音，闭环处理，包括复现、定位、改动、验证和更新演示材料。

### 2.3 沟通中出现分歧

就事论事，将分歧拆开：目标、范围、验收标准、技术方案，还是时间安排。然后用事实材料对齐，例如样本文档、接口返回、失败截图、测试输出和风险列表。

推进节奏上，给出可选方案和建议取舍。决策一旦确认，就写到文档或 issue 里，减少反复拉扯；未决事项也要标明负责人和截止时间。

### 2.4 多件事情同时压过来

按“交付、正确性、体验、文档补充”的顺序排队。开发、排查、测试、文档不能完全串行, 需要借助AI进行记录和分析，要能追踪。

AI工具适合并行处理低风险工作，例如搜索代码、归纳日志、生成测试草案、整理文档初稿和检查是否有遗漏项。但关键结果一定自己复查：改动 diff、测试命令、评估输出、敏感信息、客户口径和最终提交。

### 2.5 讲一个真实例子

黑灯电厂项目，电厂现场/设计专家/IT平台，各自负责一块，对于项目看法并不相同，项目负责人偏行业专家，但是整个项目除了模型评估，没有将整个项目协同推进的能力。行业差距大，沟通有些困难，需要我逐个建立项目知识库/数据链路/模型计算/模型训练住流程，还要建设各种小的工具，去在线调试问题，各方出现问题时可以快速对齐